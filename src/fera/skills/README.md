Built-in skills, available to all agents go here.

Other possible locations for skills are:

- `FERA_HOME/.claude/skills` for global, user-added skills available to all agents
- `WORKSPACE_DIR/.claude/skills` for skills only available to this agent.

Agent skills take precedence over global and built-in skills.
